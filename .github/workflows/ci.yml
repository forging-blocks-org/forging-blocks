name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.get-version.outputs.VERSION }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-poetry
      - use: ./github/actions/install-git-cliff
      - id: get-version
        run: echo "VERSION=$(poetry version -s)" >> "$GITHUB_OUTPUT"
      - run: poetry install --no-interaction --with dev
      - run: poetry run poe ci:check
      - run: poetry run poe test
      - name: Build and Validate Artifacts
        run: bash scripts/pipeline/validate_artifacts.sh

  validate-publish:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-poetry
      - name: Run validation script
        env:
          VERSION: ${{ needs.test.outputs.VERSION }}
          PACKAGE_NAME: forging-blocks
          IMPORT_NAME: forging_blocks
        run: bash ./scripts/validate_publish.sh

      - name: Upload to TestPyPI
        uses: pypa/gh-action-pypi-publish@v1.13.0
        with:
          repository-url: https://test.pypi.org/legacy/
          password: ${{ secrets.TEST_PYPI_TOKEN }}

      - name: Smoke Test
        run: |
          sleep 10
          pip install --index-url https://test.pypi.org/simple/ \
                      --extra-index-url https://pypi.org/simple/ \
                      forging-blocks=="${{ env.PUBLISH_VERSION }}"
          python -c "import forging_blocks; print('Success')"
